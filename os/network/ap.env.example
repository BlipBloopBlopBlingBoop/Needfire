# Needfire — access point environment (/etc/needfire/ap.env)
# Wireless interface used by needfire-ap.service. Check yours with:  ip link
# (predictable interface names like wlp2s0 are common on x86 hardware)
NEEDFIRE_AP_IF=wlan0
